#!/usr/bin/env python3
"""
Gold Agent CLI launcher.

This wrapper should behave like the installed `gold` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from gold_cli.main import main
    main()
